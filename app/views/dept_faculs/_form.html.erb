<%= form_for(@dept_facul) do |f| %>
  <% if @dept_facul.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@dept_facul.errors.count, "error") %> prohibited this dept_facul from being saved:</h2>

      <ul>
      <% @dept_facul.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
